{
  "name": "database-server",
  "version": "0.1.0",
  "description": "Database for the server template website",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node $npm_package_main",
    "dev": "nodemon $npm_package_main"
  },
  "author": "LonlyGamerX",
  "license": "MIT",
  "dependencies": {
    "better-sqlite3": "^8.6.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
